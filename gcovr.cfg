# gcovr configuration file
# Coverage scoped to MCTP source files only

filter = src/mctp/
exclude = src/tests/
# Exclude the daemon entry point from unit coverage: its main() body and the
# lambdas it posts are only reachable through the live event loop, not unit
# tests.
exclude = .*MCTPReactorMain\.cpp$
# Daemon and test targets compile the same MCTP sources with different Asio
# backends. Keep the unit-test copies and ignore duplicate, unexecuted daemon
# objects so gcovr does not merge incompatible control-flow graphs.
exclude-directory = .*/src/mctp/mctpreactor\.p$
exclude-directory = .*/src/mctp/mctpheartbeat\.p$
html = yes
print-summary = yes
decisions = yes
exclude-throw-branches = yes
exclude-unreachable-branches = yes
exclude-function-lines = yes
exclude-noncode-lines = yes
exclude-branches-by-pattern = ^\s*catch\s*\(
